# podproxy — Roadmap

Potential future features, roughly in order of interest.

- **Chapter markers / transcript proxying** — proxy chapter and transcript files referenced in the RSS feed, not just episode audio
- **Transcoding / format conversion** — re-encode episodes on the fly (e.g. for bandwidth or device compatibility)
- **DB backup export** — API endpoint or CLI flag to export a portable backup of the SQLite database
